export const ROWS = 31;
export const COLS = 61;

export const START_IDX = [Math.round(ROWS / 2), Math.round(COLS / 5)];
export const END_IDX = [Math.round(ROWS / 2), Math.round((COLS * 4) / 5)];

export const LOOP_DELAY = 3;
